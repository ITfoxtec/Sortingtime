namespace Sortingtime.Models
{
    public enum InvoiceStatus
    {
        Deleted = 10,
        Credited = 20,
        Created = 100,
        Resending = 200,
        Send = 500,
        Resend = 600,
    }
}
